import { makeStyles } from '@mui/styles';

export default makeStyles((theme) => ({
  paper: {
    padding: '10px 30px',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
  },
}));
